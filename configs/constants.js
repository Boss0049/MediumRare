// Database
const DB_HOST = "localhost";
const DB_USER = "root";
const DB_PASSWORD = "";
const DB_DATABASE = "mini-medium";
const DB_PORT = 3306;

// Server
const SERVER_PORT = 3000;

// Authentication
const JWT_SECRET = "2XfHoRtorX1OmeqX";
const JWT_EXPIRE_TIMEOUT = 3600;

// ENum
const ADMIN_ROLE = "admin";
const USER_ROLE = "user";

module.exports = {
  DB_HOST,
  DB_USER,
  DB_PASSWORD,
  DB_DATABASE,
  DB_PORT,
  SERVER_PORT,
  JWT_SECRET,
  JWT_EXPIRE_TIMEOUT,
  ADMIN_ROLE,
  USER_ROLE,
};
